# Preview check

Temporary marker file used to trigger the first Vercel **Preview** deployment
for the `staging-preview` branch. It has no effect on the site and can be
deleted once the preview URL is confirmed working.
